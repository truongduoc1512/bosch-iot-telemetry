version: '3.8'
services:
  jenkins-controller:
    image: jenkins/jenkins:lts
    container_name: jenkins_controller
    restart: unless-stopped
    ports:
      - "8081:8080" # Map ra port 8081
      - "50000:50000"
    volumes:
      - jenkins_home:/var/jenkins_home
volumes:
  jenkins_home: